export class Vehicle {
constructor (license, model, latLong) {
        this.license = license;
        this.model = model;
        this.latLong = latLong;
    }
}

// Three propoerites that all cars and drones have in common stored on instance vechile.